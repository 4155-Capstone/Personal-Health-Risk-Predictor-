"""
src package initializer

This file makes the `src` directory a Python package so `ui` can be imported as
`src.ui` or, when running from the repository root, `ui` if the working
directory is adjusted accordingly.
"""

# package metadata (optional)
__all__ = []
